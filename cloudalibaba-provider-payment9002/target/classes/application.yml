server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    # 配置 nacos
    nacos:
      discovery:
        server-addr: localhost:8848 # 配置 nacos 地址

# 暴露监控
management:
  endpoints:
    web:
      exposure:
        include: '*'